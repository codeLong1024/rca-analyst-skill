# RCA 工作流

## 铁律
1. 无证据不下结论，每条证据标注来源（file:line / 日志时间戳 / Issue 原文）。
2. 静态推断必须被运行时证据证实，否则不得锁定根因。
3. 先排除，再判定。
4. 查询失败即跳过，不重试，显式降级为 `Unknown_<标识>` 占位。

## 第一步：理解问题
- 收集关键信息：现象、触发条件、影响范围、事件时间窗（用绝对时间，不用"最近 N 小时"）。
- 检查已有 `.rca/` 报告避免重复分析；信息不足时标 `Unknown_<标识>` 继续。

## 第二步：根因分析（5 Whys）

### 2.1 架构分析
绘制数据流图（纯文本），标注关键组件、数据流向、瓶颈点、单点故障。

### 2.2 证据采集（四象限）
| 象限 | 来源 | 说明 |
|---|---|---|
| 语义 | 文档 / Issue 描述 / 知识库 | 补全背景，查不到填 `Unknown_<标识>` |
| 结构 | 代码 / 配置 / 拓扑 | 检索预算 ≤10 次，防无限螺旋 |
| 运行时 | 日志 / trace / 监控 | 静态推断定论前**必查** |
| 变更 | git log / 发布记录 | 定位到 file:line 后才查，不计入置信度 |

### 2.3 逐层追问
```
现象：接口返回 500 错误
→ 为什么？数据库连接池耗尽
  → 为什么？慢查询占用连接时间过长
    → 为什么？缺少联合索引
      → 为什么？表结构设计时未考虑该查询场景
        → 为什么？需求评审时未覆盖该业务场景 ← 根因
```
- 每一层都要有证据（标注象限来源）
- 区分"直接原因"和"根本原因"，识别多个根因分支

### 2.4 判定（否定优先，显式编号）
1. 逐项排除非问题场景：误报、预期行为、环境差异
2. 全部排除后，将根因映射到封闭枚举：
   | 枚举 | 含义 |
   |---|---|
   | fix_code | 改代码 |
   | fix_config | 改配置 |
   | rate_limit | 限流（须有运行时日志佐证） |
   | whitelist | 误报豁免（第 1 步全部排除 + 语义属预期行为） |

### 2.5 置信度
| 得分 | 等级 | 处置 |
|---|---|---|
| 3~4 | high | 建议采纳 |
| 2 | medium | 参考，需确认 |
| 0~1 | low | 仅归档 |

语义证据无法补全 → 封顶 medium。变更信息不计分。

## 第三步：方案建议
**边界**：只改实际触发问题的入口；优先复用已有机制；新增抽象前先算 ROI。

每个方案包含：思路 + 改动范围 + 风险 + 工作量（人天）。提供：
- **推荐方案** — 优势最大
- **备选方案** — 可接受的替代
- **临时方案** — 过渡止血，注明局限性

多因场景：主动作选最快止血，其余按优先级列入路线图。

## 第四步：实施路线图
- **短期（1-2 周）**：最小可用修复
- **中期（1-2 个月）**：完善与测试
- **长期**：架构优化，沉淀为技术规范

## 第五步：输出报告

```markdown
# [Issue #XXX] 根因分析报告：[问题标题]

> 分析时间：YYYY-MM-DD | 问题来源：GitHub / Gitee / GitLab / 本地描述 | 置信度：high/medium/low

## 一、问题描述
## 二、根因分析
### 2.1 架构分析（数据流图）
### 2.2 证据采集（四象限表）
### 2.3 5 Whys 分析（逐层 Why + 证据）
### 2.4 判定结果（枚举 + 置信度）
## 三、方案建议（推荐 / 备选 / 临时）
## 四、实施路线图（短期 / 中期 / 长期）
## 五、总结
> 根因：一句话 | 推荐方案：一句话 | 需人工确认：决策点
## 附录（代码位置 / 关联 Issue / 参考资料）
```

### 文件管理
1. **仅写入 `.rca/` 目录**，绝不污染项目其他位置
2. **命名**：有 Issue 编号用 `ISSUE_{编号}_根因分析报告.md`，否则用 `{YYYY-MM-DD}_{关键词}_根因分析报告.md`
3. **gitignore**：优先检查全局 excludesfile 是否已包含 `.rca/`，若未配置则提示用户执行：`git config --global core.excludesfile ~/.gitignore_global && echo ".rca/" >> ~/.gitignore_global`
4. **更新已有报告**：同一问题优先更新，追加变更记录；仅当问题性质根本变化时才新建
