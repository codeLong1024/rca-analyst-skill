# RCA 工作流

## 第一步：理解问题

- 收集 Issue 或用户描述中的关键信息：现象、触发条件、影响范围
- 探索代码库中相关实现，检查已有 `.rca/` 报告避免重复分析
- 信息不足时标注 `[待确认]` 继续分析

## 第二步：根因分析（5 Whys）

### 2.1 架构分析

绘制数据流图（纯文本），标注关键组件、数据流向、瓶颈点、单点故障。

### 2.2 逐层追问

从表面现象出发，连续追问"为什么"直到根因：

```
现象：接口返回 500 错误
→ 为什么？数据库连接池耗尽
  → 为什么？慢查询占用连接时间过长
    → 为什么？缺少联合索引
      → 为什么？表结构设计时未考虑该查询场景
        → 为什么？需求评审时未覆盖该业务场景 ← 根因
```

- 每一层都要有代码或日志证据
- 区分"直接原因"和"根本原因"
- 识别多个根因分支

### 2.3 技术障碍清单

| 障碍项 | 描述 | 严重程度 | 证据 |
|--------|------|---------|------|

## 第三步：方案建议

每个方案包含：思路 + 改动范围 + 风险 + 工作量（人天）。提供：
- **推荐方案** — 优势最大
- **备选方案** — 可接受的替代
- **临时方案** — 过渡止血，注明局限性

## 第四步：实施路线图

- **短期（1-2 周）**：最小可用修复
- **中期（1-2 个月）**：完善与测试
- **长期**：架构优化，沉淀为技术规范

## 第五步：输出报告

### 报告结构

```markdown
# [Issue #XXX] 根因分析报告：[问题标题]

> 分析时间：YYYY-MM-DD | 问题来源：GitHub / Gitee / GitLab / 本地描述

## 一、问题描述
## 二、根因分析
### 2.1 架构分析（数据流图）
### 2.2 5 Whys 分析（逐层 Why + 证据）
### 2.3 技术障碍（表格）
## 三、方案建议（推荐 / 备选 / 临时）
## 四、实施路线图（短期 / 中期 / 长期）
## 五、总结
> 根因：一句话 | 推荐方案：一句话 | 需人工确认：决策点
## 附录（代码位置 / 关联 Issue / 参考资料）
```

### 文件管理

1. **仅写入 `.rca/` 目录**，绝不污染项目其他位置
2. **命名**：有 Issue 编号用 `ISSUE_{编号}_根因分析报告.md`，否则用 `{YYYY-MM-DD}_{关键词}_根因分析报告.md`
3. **gitignore**：推荐全局忽略，避免修改项目 `.gitignore` 与上游冲突。优先检查全局 excludesfile 是否已包含 `.rca/`，若未配置则提示用户执行：`git config --global core.excludesfile ~/.gitignore_global && echo ".rca/" >> ~/.gitignore_global`
4. **更新已有报告**：同一问题优先更新，追加变更记录；仅当问题性质根本变化时才新建
